---
project: sportking
type: log
audience: both
updated: 2026-07-06
summary: Append-only activity log — every piece of work, one dated line, newest first.
---

# sportking — log

Every completed piece of work gets one line (newest first). Big features also live
on [[board]]; this is the full history.

## 2026-07-06
- **Product-list enrichment pipeline built (`scripts/reporting/enrich.py`) + EXIT-feed question resolved.** User: **EXIT has stopped dealing with us** → no EXIT feed/images ever coming (kills the "add EXIT dealer feed" backlog item). New approach fills name+category+images per scanned EAN from three deterministic tiers, highest first: (1) `products/ean-overrides.csv` — a committed, accumulating knowledge file (manual + past web-resolved products; also a correction lever over Allegro); (2) **Allegro catalog** live API (strict EAN match via `allegro_draft.match_product`) — the channel we sell on, gives Polish name, leaf category, and originals → `products/photos-allegro/<EAN>/`; (3) BERG dealer feed name/category. Leftovers → `products/dark-todo.csv` (shares the overrides schema; fill blanks, paste back, rerun — zero re-lookups). Prices skipped (matched on Allegro). Batch-2 result: **107 products → 33 override · 56 Allegro · 17 feed · 1 dark**, 349 images on disk. Only `8731869663717` unidentifiable (absent from every barcode DB). The 33 overrides were seeded THIS session by 4 parallel web-lookup agents over the 34 dark EANs (mostly EXIT modular carton EANs + `4260237*` = small foot/BAFF beatBOX Cajons, NOT Hudora). **Future runs = script-only; an agent just oversees `enrich.py` and resolves the tiny new-dark tail into `ean-overrides.csv`.** Confirms the modular-EAN finding (Galaxy basketball, Loft playhouse, Silhouette trampoline all ship as per-carton EANs). → [[architecture]], [[board]].
- **Second warehouse scan batch landed** (`new_raport.odc`, a LibreOffice Calc export mislabeled `.odc`) — a **full recount**, not incremental. Converted to xlsx, archived to `products/scans/2026-07-06-full.xlsx`, set as the active `products/list.xlsx`. Result: 229 scans, 107 unique EANs, 40 matched to the BERG feed, 67 unmatched, 0 bad scans → `report/2026-07-06_18-02-34.xlsx`. → [[board]].
- **Reorganized the 9 root-level Python scripts into concern-based folders**: `allegro/` (auth + draft offers), `shop/` (BaseLinker import + PrestaShop push), `reporting/` (scan-report + BERG feed parsing), `scraping/` (photo sources), `lib/` (shared stdlib xlsx helper). Fixed each script's `ROOT` computation and `sys.path` wiring so cross-imports and repo-root-relative paths (`.env`, `products/`, `library/…/backups`, `report/`) still resolve correctly regardless of folder depth; verified every moved script imports cleanly and re-ran scan-report end-to-end. → [[architecture]] (Tooling).
- **Built `reporting/ingest-scan.py`**, the standing workflow for future scan pastes: user pastes the raw scan export at repo root, script converts/archives it to `products/scans/<date>-<full|append>.xlsx`, updates `products/list.xlsx` (`--full` replaces it — a complete recount; `--append` concatenates — new stock on top of the running total), removes the root paste, and auto-runs `scan-report.py`. Verified both modes (full + append) in an isolated test dir before running for real. Consolidated this session's duplicate archives/reports created while testing. → [[architecture]] (Tooling).
- **Added [[user-board]]** — a personal kanban (`audience: user`) for todos only the user can act on, separate from [[board]] (shared/Claude-actionable plan). Migrated the two items from the user's root scratch file `zrobic` into it; the scratch file is now redundant and removed.
- Checked Obsidian plugin needs: `obsidian-kanban` was already installed + enabled in this vault (used by `board.md`); no action needed for the new `user-board.md`. Flagged a gap upstream: `~/ai-workflow`'s `bootstrap.sh` scaffolds `board.md` with `kanban-plugin` frontmatter but never installs the plugin itself — logged as a suggestion in `~/ai-workflow/suggestions.md` (2026-07-06) rather than fixed here, since it's a general-workflow issue, not sportking-specific.
- **Nested all code under `scripts/`** (user request: separate scripts from product data at the top level) — `allegro/`, `shop/`, `reporting/`, `scraping/`, `lib/` moved from repo root into `scripts/`. Updated every script's `ROOT` computation (now three levels up) and `sys.path` wiring, fixed docstring usage examples; re-verified every script imports cleanly and re-ran `scan-report.py` end-to-end from the new path. → [[architecture]] (Tooling).

## 2026-07-04
- Added `User-Agent: sportking-sync/v1.0 (+https://sportking.pl/sportking-app)` to every Allegro REST request — all 5 sites across `allegro_auth.py` (`_post`, `_bearer_get`) and `allegro_draft.py` (`_call`, `_refresh`); defined as a `UA` constant per file. Verified live via read-only `./allegro_auth.py whoami` (auth OK, 105 offers). Identifies our traffic to Allegro (recommended practice; helps avoid generic-client rate limiting).
- Analyzed BERG data inconsistencies across feed/shop/veloking/Allegro (user prepping to group with incoming EXIT catalog + BERG photos). Key: **article number is the only consistent join key**; feed names multi-language (~1915/6023 EN/DE/NL), EAN drifts, 3 scanned items not in 2026 feed, 2335/6023 priced (260 ACTIVE unpriced), status mostly blank, 1 data error (#BERG Rebl vs Reppy Rider). → [[architecture]] (BERG data inconsistencies section). User removed the 28 BL-catalog imports (clean slate for proper grouping).

## 2026-07-03
- Backed up the 18 `sklep_veloking` Allegro offers → backups/2026-07-03-veloking-offers.json (transcribed from BaseLinker panel, all EAN/SKU verified vs presta, 0 mismatches; can't API them — no BL offer method, Allegro token is Inkontor's). **Flagged:** 12/18 veloking offers source from presta products the zero-out set to 0; offer qty appears independent of presta stock (10≠7) so likely safe, but decision pending user (restore / watch / intended). Held — made no further presta writes while user away.
- **Zeroed legacy PrestaShop stock** (reconstruction cleanup). User set BaseLinker "Stany" sync = Base.com→sklep (live) so catalog stock auto-pushes to presta for LINKED products — but that only covers catalog-linked items and the external-storage API write stays `write:false`, so the mass zero-out couldn't go via BaseLinker. Did it via SSH direct DB (user chose "only zero, leave scanned to sync"): transactional UPDATE of `ps_stock_available` set 381 non-scanned rows (incl. 3 negatives) → 0; left the 15 scanned-in-presta rows untouched for the sync. products_with_stock 392→14. Guarded (abort if affected>381), backup at backups/2026-07-03-presta-stock-before.jsonl (full rollback). **Open: verify the scanned catalog products are LINKED to the shop so the sync actually pushes their qty** (e.g. Biky White presta=4 vs scanned=5). → [[architecture]], [[board]].
- **Imported all 32 scanned products into BaseLinker catalog 4002** (`baselinker_import.py`, keyed by EAN so BL matches the Allegro offers). 28 created + 4 already present by EAN = 45 total (was 17). Stock = scanned qty in warehouse `bl_5086` (BL's internal virtual warehouse), no price. 8 EXIT items imported with placeholder "EAN <n>" names (no feed yet). Catalog backed up before write; created product_ids in backups/2026-07-03-baselinker-imported.json (rollback: deleteInventoryProduct). Re-confirmed: BaseLinker API has NO Allegro offer-creation method (Allegro = order source only). Match completes when Allegro drafts are published (BL links by EAN). → [[architecture]], [[board]].
- **Created Allegro drafts on sklep_Inkontor** (first live marketplace write). Direct Allegro REST API, OAuth device-flow connected. `allegro_draft.py`: matches scanned EAN → Allegro catalog product (STRICT, EAN-verified; no fuzzy fallback), creates price-less UNPUBLISHED (INACTIVE) product-offers with scanned qty as stock. Result: 18 of 32 scanned products matched Allegro's catalog → all 18 drafted (17 via API + 1 "Buzzy Bloom" already existed on the account). 14 unmatched (EXIT items + a few discontinued BERG) can't be drafted this way — no Allegro catalog product. Dedupe removed 2 self-duplicates (delete = `DELETE /sale/offers/{id}`, 204; product-offers path 405s). All 17 my-draft ids tracked in `library/sportking/backups/2026-07-03-allegro-inkontor-drafts.json` (rollback). Drafts have NO price + are unpublished → safe, need prices+publish in panel. → [[architecture]], [[board]].
- Allegro drafting: BaseLinker API can't create offers (panel-only) + no Allegro OAuth yet, so pivoting to direct Allegro REST API for `sklep_Inkontor` (26050, user's choice). Built `allegro_auth.py` (device-code OAuth: login/refresh/whoami; tokens git-ignored). Next: user registers Allegro app → client id/secret in .env → authorize → then category map + prices + unpublished draft offers (dry-run first) → [[architecture]], [[board]].
- Dealerzone photo pipeline built (`scrape-dealerzone-photos.py`). Findings: portal = ASP.NET (dealerzone.net), login works with quote-stripped creds from .env (UserName=email + antiforgery); **product images are PUBLIC** at `/product/image/large/<article>_<n>.jpg`, max res **1024×768** (no bigger size exists). Article-keyed, no page scraping. Batch 1: 6/19 from Dealerzone (older stock absent), 16/19 with a photo combined w/ legacy export. Wired both photo sources into scan-report counts. Brand-portal Marvia deferred for true high-res → [[architecture]], [[board]].
- BERG dealer feed (`products/dealers/berg-2026.xlsx`) integrated. Rewrote scan-report into an enriched **multi-page xlsx** report (Summary/Inventory/Unmatched/Bad scans) + persistent store-agnostic `products/berg-master.csv` (6023 products); new `berg_feed.py` (links on article no., category via pricelist+prefix inference, price from pricelist col D) and stdlib `lib_xlsx.py` (read+write, no openpyxl). Batch-1 now 19 matched (feed > dead-shop manifest). Feed has NO product images.
- Photos investigation: built `scrape-berg-photos.py` (Shopify products.json, article-keyed via variant.sku + filename). Dead end for now — us.bergtoys.com carries only 53 articles (0 of our older/discontinued inventory); global site moved to berg.com (non-Shopify). Conclusion: photo source = BERG **dealer media bank** (ask user) + existing shop export (15/19 scanned covered) + Allegro EAN auto-fill. Decision pending → [[board]].
- Diagnosed unmatched-EAN cause: EXIT products are modular (component boxes each with own EAN; sellable unit = assembly, keyed by EXIT article no. e.g. 46-15-10-00). Proved from scan batch: 8718469469215 base×7 + 8719874704397 backboard×7 = 7 complete Galaxy basketball sets. BL inventory 4002 nearly empty (17 products, none unmatched) → not a data source. Of 17 unmatched: ~10 EXIT-family (bundle case), 7 BERG (just not-in-old-shop). Proposed: EXIT/BERG dealer feed → EAN→article→set master + bundle map + scan-report roll-up → [[architecture]] (EXIT modular section). Awaiting user on feed availability.
- Built `scan-report.py` (repo root, stdlib-only xlsx parser): products/list.xlsx → `report/<date-time>.csv` per scan session; verified identical to the manual batch-1 analysis; replaces one-off products/scan-report.csv → [[architecture]] (Tooling).
- Analyzed first warehouse scan batch (`products/list.xlsx`, 79 rows) → `products/scan-report.csv`: 32 unique EANs / 73 units; 15 EANs matched to legacy photos+names, 17 unknown (mostly 8718469* = likely EXIT Toys, never in old shop); 6 bad scans are `YYMMDD-serial` production codes. Veloking-snapshot suggestion declined (for now). User will zero presta stock + sync from Allegro/BaseLinker himself → [[_moc]].
- User restructured: photo export now lives at `products/photos/<EAN>/` + `products/manifest.csv` (was `allegro-photos/`; old manual `photos/` deleted). Refs updated in [[_moc]], [[architecture]], [[board]]; manifest.csv now git-tracked, photos ignored.
- Plan pivot recorded: full catalog reconstruction — presta products discarded, only sklep_veloking listings stay, warehouse scanner count → EAN list incoming as source of truth → [[_moc]] (key decision), [[architecture]] (banner), [[board]] (cards).
- VPS access live (dedicated key). Read-only recon: PrestaShop 9.0.3 at /home/henrik/sportking (colleague-maintained, hands off), DB sportkingdbs. Exported all 1355 active-product images → `allegro-photos/<EAN>/NN.jpg` + manifest.csv (508 dirs; 38 no-EAN use name slugs, 7 dup-EAN split as `<ean>-dup-<id>`, 3 products imageless). Method: JSONL dump of ps_product/ps_image via PHP over SSH → rsync originals → build_tree.py; server untouched → [[architecture]].
- Access batch: enumerated BaseLinker order sources — 3 Allegro accounts (sklep_sportking 4448, sklep_Inkontor 26050, sklep_veloking 50219) + shop sportking_pr 30180 (product storage read-only) → [[architecture]]. Generated dedicated VPS key `~/.ssh/sportking_claude` (user's ssh-copy-id pending). Added VPS safety rules (no deploy script → backup-before-change, exact commands) → [[_meta/project-rules]]. Tracked library/workflow/.
- BaseLinker API token saved to `.env` (`baselinker_api`, git-ignored) and verified read-only: inventory "Sportking" id 4002 (pl, warehouse bl_5086) → [[architecture]].
- Public-site audit: sportking.pl live (Apache/Debian, LE cert fresh, ~1.1s TTFB), BERG/EXIT/Didakites/Gepetto catalog visible → [[architecture]]. Revival plan: audit-first; BaseLinker+Allegro access granted, token pending.
- Added live-system safety rules (read-only default, backup-before-write, dry-run confirm) → [[_meta/project-rules]].
- Captured baseline project context from the user (PrestaShop on Hetzner, BaseLinker, Allegro, BERG/EXIT partners) → [[_moc]], new [[architecture]] draft.
- Project bootstrapped from `~/ai-workflow` (vault skeleton, _meta symlinks, memory wiring).
